import { expect } from 'chai'

describe('smoke test', () => {
    it('checks equality 1', () => {
        expect(true).to.be.true
    })

    it('checks equality 2', () => {
        expect(true).to.be.true
    })

    it('checks equality 3', () => {
        expect(true).to.be.true
    })
})
